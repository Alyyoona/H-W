'''Написать функцию, которая принимает как аргумент алфавит последовательности, 
а возвращает функцию получения статистики встречаемости символов в последовательности'''
statistics = count_chars('abcd')
def count_chars(alphabet):
    char_counts = {char: 0 for char in alphabet}

    def get_char_statistics(sequence):
        for char in sequence:
            if char in char_counts:
                char_counts[char] += 1
            else:
                print(f"Ignoring character: {char}")

        return char_counts

    return get_char_statistics

print(statistics('aabbccdd'))  # {'a': 2, 'b': 2, 'c': 2, 'd': 2}
print(statistics('abcde'))  # {'a': 1, 'b': 1, 'c': 1, 'd': 1, 'e': 0}
